# sub2api 领域术语

本文件记录项目内需要保持一致的领域语言。当前先收录 OpenAI 调度上下文中已经明确的术语。

## OpenAI 调度

**高级调度引擎**：
负责执行 OpenAI 账号候选过滤、粘性处理、策略计算、并发槽获取和失败回退的底层选号引擎。它是实现能力，不代表某个具体流量策略。
_避免使用_：高级调度策略、自动调度

**自动调度策略**：
基于健康、性能、成本、容量和会话亲和性计算候选账号顺序及目标流量份额的策略。只有满足全局和分组门控时才可影响真实流量。
_避免使用_：高级调度、自动探测

**有效调度策略**：
某次请求在全局开关、分组开关、运行模式、影子模式和降级状态共同作用后实际采用的策略。
_避免使用_：页面配置模式

**调度分区**：
可以直接进行性能和成本比较的一组候选，边界为分组、模型族、上游端点和传输协议的组合。
_避免使用_：综合账号排名、全局排名

**综合账号排名**：
指定分组内按真实账号唯一展示的综合排序，汇总该账号参与的多个调度分区。同一账号在一份排名中只能出现一次。
_避免使用_：调度分区排名、模型排名

**目标流量份额**：
自动调度策略根据当前候选状态为新会话计算的预期首选比例，不包含历史会话粘性造成的偏移。
_避免使用_：实际占比、固定配额

**综合目标流量份额**：
综合账号排名中按各调度分区流量结构汇总的账号预期首选比例，用于与综合实际流量份额比较。
_避免使用_：固定账号权重、分区目标流量份额

**分区实际流量份额**：
指定统计窗口内，某账号真实承接请求数占所在调度分区请求总数的比例。
_避免使用_：目标概率、调度权重

**综合实际流量份额**：
指定统计窗口内，某账号真实承接请求数占所选分组请求总数的比例，用于综合账号排名。
_避免使用_：分区实际流量份额、目标流量份额

**强亲和**：
为保证上下文正确性必须优先回到原账号的关联关系，典型来源是 `previous_response_id`。
_避免使用_：普通粘性

**软亲和**：
倾向继续使用原账号、但在延迟、排队、错误或限流明显恶化时允许逃逸的会话关系，典型来源是 `session_hash`。
_避免使用_：强粘性

**影子决策**：
只计算并记录新策略结果、不改变真实选号结果的调度决策。
_避免使用_：灰度流量

**候选置信度**：
由健康样本数量、新鲜度和来源共同决定的调度信息可信程度，用于限制低样本账号获得过高流量。
_避免使用_：健康分
